#include <stdio.h>
int main()
{
	int p[100] = {}, pos = 0, n;
	for (n = 1; n <= 1000; n++)
	{
		
	}
	int i;
	scanf("%d", &k);
	while (k > 1)
	{
		if (k%prime[i] == 0)
		{
			printf("%d\n", prime[i]);
			k /= prime[i];
		}
		else
		{
			i++;
		}
	}
	printf("0");
	return 0;
}
